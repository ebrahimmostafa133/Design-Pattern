Bridge Pattern
==============

Purpose
-------
Decouple an abstraction from its implementation so the two can vary independently. Useful when both
abstraction and implementation should be extensible.

Files: simple remote/device bridge example.
